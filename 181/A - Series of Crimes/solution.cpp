#include <iostream>#include <vector>#include <string>#include<math.h>#include<algorithm>#include <numeric>#include<map>#include<unordered_map> using namespace std;  int main() {       ios::sync_with_stdio(false);    cin.tie(nullptr);    /* FILE* fin;    FILE* fout;     freopen_s(&fin, "input.txt", "r", stdin);    freopen_s(&fout, "output.txt", "w", stdout);*/            int n, m;        cin >> n >> m;         int x = 0, y = 0;         for (int i = 0; i < n; i++) {            string s;            cin >> s;            for (int j = 0; j < m; j++) {                if (s[j] == '*') {                    x ^= i;                    y ^= j;                }            }        }         cout << x + 1 << " " << y + 1 << endl;          return 0;    }