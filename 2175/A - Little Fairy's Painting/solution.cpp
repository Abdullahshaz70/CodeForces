#include <iostream>#include <vector>#include <string>#include<math.h>#include<algorithm>#include <numeric>#include<set> using namespace std; int main() {       ios::sync_with_stdio(false);    cin.tie(nullptr);    /* FILE* fin;    FILE* fout;     freopen_s(&fin, "input.txt", "r", stdin);    freopen_s(&fout, "output.txt", "w", stdout);*/     int t;    cin >> t;    while (t--) {         int size;        cin >> size;         vector<int> r;         while (size--) {            int n;            cin >> n;            r.push_back(n);        }         set<int> s(r.begin(), r.end());           while (find(r.begin(), r.end(), s.size()) == r.end()) {            s.insert(s.size());        }            cout << s.size() << endl;    }         return 0;}