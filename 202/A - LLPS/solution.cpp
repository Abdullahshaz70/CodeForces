#include <iostream>#include <vector>#include <string>#include<math.h>#include<algorithm>#include <numeric>#include<map>#include<unordered_map> using namespace std; string func(const string& q) {       string result = "";    int size = q.size();     unordered_map <char, int> map;        for (int i = 0; i < size; i++)        map[q[i]]++;     /*for (auto p : map)        cout << p.first << " " << p.second << endl;*/     char max_elem = *max_element(q.begin(), q.end());        int count = map[max_elem];     for (int i = 0; i < count; i++)        result += max_elem;        return result;}int main() {       ios::sync_with_stdio(false);    cin.tie(nullptr);    /* FILE* fin;    FILE* fout;     freopen_s(&fin, "input.txt", "r", stdin);    freopen_s(&fout, "output.txt", "w", stdout);*/     string c;    cin >> c;     cout << func(c);     return 0;}