#include <iostream>#include <vector>#include <string>#include<math.h>#include<algorithm>#include <numeric>#include<map>#include<unordered_map> using namespace std; int func(const vector<vector<int>>& matrix , const int& size) {     int sum = 0;     for (int i = 0; i < size; i++) {        for (int j = 0; j < size; j++) {             if (i == j || i == size / 2 || j == size / 2 || i + j == size - 1)                sum += matrix[i][j];         }    }        return sum;} int main() {       ios::sync_with_stdio(false);    cin.tie(nullptr);    /* FILE* fin;    FILE* fout;     freopen_s(&fin, "input.txt", "r", stdin);    freopen_s(&fout, "output.txt", "w", stdout);*/        int size;    cin >> size;     vector<vector<int>> matrix(size, vector<int>(size, 0));     for (int i = 0; i < size; i++) {        for (int j = 0; j < size; j++) {            int n;            cin >> n;            matrix[i][j] = n;        }    }     cout << func(matrix, size)<<endl;          return 0;    }