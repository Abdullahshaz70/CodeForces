#include <iostream>#include <vector>#include <string>#include<math.h>#include<algorithm>#include <numeric>#include<map>#include<unordered_map> using namespace std;  int main() {       ios::sync_with_stdio(false);    cin.tie(nullptr);    /* FILE* fin;    FILE* fout;     freopen_s(&fin, "input.txt", "r", stdin);    freopen_s(&fout, "output.txt", "w", stdout);*/        int size;    cin >> size;    int k;    cin >> k;    k = k - 1;    vector<int> contestents;     for (int i = 0; i < size; i++) {        int n;        cin >> n;        contestents.push_back(n);    }        int count = 0;     for (int i = 0; i < size; i++) {        if (contestents[k] <= contestents[i] and contestents[i] > 0)            count ++;    }    cout << count << endl;      return 0;}