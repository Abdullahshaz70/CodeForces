#include <iostream>#include <vector>#include <string>#include<math.h>#include<algorithm>#include <numeric>using namespace std; int gcd(int a, int b) {    return b == 0 ? a : gcd(b  , a % b);} int main() {       ios::sync_with_stdio(false);    cin.tie(nullptr);     /*FILE* fin;    FILE* fout;     freopen_s(&fin, "input.txt", "r", stdin);    freopen_s(&fout, "output.txt", "w", stdout);*/     int t;    cin >> t;    while (t--) {        int l, a, b;         cin >> l >> a >> b;         int cycle = l / gcd(l, b);         int max = 0;         for (int i = 1; i <= cycle; i++) {            int n = (a + (i*b)) % l;            if (n > max)                max = n;        }         cout << max << endl;            }    return 0;}