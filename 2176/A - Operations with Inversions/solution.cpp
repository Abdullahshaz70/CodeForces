#include <iostream>#include <vector>#include <string>#include<math.h>#include<algorithm>#include <numeric>using namespace std; int gcd(int a, int b) {    return b == 0 ? a : gcd(b  , a % b);} int main() {       ios::sync_with_stdio(false);    cin.tie(nullptr);     /*FILE* fin;    FILE* fout;     freopen_s(&fin, "input.txt", "r", stdin);    freopen_s(&fout, "output.txt", "w", stdout);*/     int t;    cin >> t;    while (t--) {                int size;        cin >> size;         vector<int> r;         while (size--){            int n;            cin >> n;            r.push_back(n);        }                int count = 0;                for (int i = r.size()-1; i >= 0; i--) {             int max_befor = *max_element(r.begin(), r.begin()+i);            if (r[i] < max_befor)                count++;         }         cout << count << endl;     }    return 0;}